import 'package:flutter/material.dart';

class NfcInfoPage extends StatelessWidget {
  const NfcInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("NFC Info")),
      body: const Center(child: Text("NFC details coming soon...")),
    );
  }
}
